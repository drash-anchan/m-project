# Keep these keys on the backend only. NEVER prefix them with VITE_.
OPENWEATHER_API_KEY=
SENSOR_API_URL=
SENSOR_API_KEY=
CWC_API_URL=
REQUEST_TIMEOUT_SECONDS=12
CACHE_TTL_SECONDS=300
# Set true only if you need an explicitly-labelled offline demo fallback.
DEMO_FALLBACK=false
# Key for POST /api/sensors/ingest. Leave blank to disable authentication (demo only).
SENSOR_INGEST_KEY=

# Optional Generative AI (OpenAI-compatible endpoint)
GENAI_API_KEY=
GENAI_API_URL=
GENAI_MODEL=gpt-4o-mini
