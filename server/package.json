{
  "name": "disaster-platform-alerts-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Real SMS + AI voice-call dispatch backend for the Disaster Response Platform. Holds the telecom provider credentials so they never touch the browser.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "twilio": "^5.3.0"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
